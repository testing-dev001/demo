version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd demo/my-app
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: demo/my-app/dist
    files:
      - '**/*'
  cache:
    paths:
      - demo/my-app/node_modules/**/*
